<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginBottom="2dp"
                android:layout_marginLeft="2dp"
                android:layout_marginRight="2dp"
                android:orientation="vertical"
                android:paddingLeft="12dp"
                android:paddingRight="12dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp">

    <TextView
        android:id="@+id/host_name"
        style="@style/ListItemFont"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/host_ip"
        style="@style/ListItemFont"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/host_name"
        android:textSize="12sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/host_mac"
        style="@style/ListItemFont"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/host_ip"
        android:layout_toRightOf="@id/host_ip"
        android:gravity="end"
        android:textSize="12sp"/>

    <TextView
        android:id="@+id/host_vendor"
        style="@style/ListItemFont"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/host_ip"
        android:textSize="12sp"/>

</RelativeLayout>
